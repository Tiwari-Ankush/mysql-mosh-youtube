-- SELECT first_name , last_name
-- SELECT first_name , last_name, points,points+10
-- SELECT first_name , last_name, points, (points + 10)*100 AS discount_fact
-- FROM customers

-- SELECT state
select distinct state
from customers